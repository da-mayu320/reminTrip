<!DOCTYPE html>
<html>
  <head>
    <title>ReminTrip</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header>
        <nav>
          <%= button_to "ログアウト",
                        destroy_user_session_path,
                        method: :delete,
                        class: 'nav-link px-3' %>
        </nav>
      </header>
    <% end %>

    <%= yield %>
  </body>
</html>
